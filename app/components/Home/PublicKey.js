// @flow
import React, { Component } from 'react';
import PublicKeyContainer from '../../containers/Home/PublicKey';

class PublicKey extends Component<Props> {
    render() {
        return (
          <PublicKeyContainer />
        );
    }
}

export default PublicKey;